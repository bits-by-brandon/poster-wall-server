export const port: number = parseInt(process.env.PORT) || 3000;
export const appName: string = process.env.APP_NAME || "application";
export const dbHost: string = process.env.DB_HOST || "localhost";
export const dbPort: number = parseFloat(process.env.DB_PORT) || 28015;
